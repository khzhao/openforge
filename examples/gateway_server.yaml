# Copyright 2026 openforge

# Server-owned config loaded when the gateway process starts.
# This file is not sent by clients.

data:
  path: /tmp/openforge-gateway.db

gateway:
  host: 127.0.0.1
  port: 8000

cluster:
  num_nodes: 1
  gpus_per_node: 4
  cpus_per_node: 16
